import {Button,TextField} from '@mui/material';import {useLearningSession} from '../../state/LearningSessionProvider';import {sjLessonPacks} from '../../adapters/local-demo/localDemoFixtures';import {TtsControls} from './TtsControls';
export function RestatementCheck():JSX.Element{const{state,dispatch}=useLearningSession();const valid=state.restatement.trim().length>=8&&!/^(学完了|我会了)[。！!]?$/u.test(state.restatement.trim());const pack=state.lessonId?sjLessonPacks[state.lessonId]:undefined;const prompt= pack?.restatementPrompt??'请复述：遇到分数加减，第一步判断什么？';return<section className="restatement"><p className="eyebrow">进入 Quiz 前的证据门禁</p><h2>{prompt}</h2><TtsControls text={prompt} auto label="朗读复述提示"/><TextField fullWidth multiline minRows={3} label="用自己的话复述" value={state.restatement} onChange={(e)=>dispatch({type:'SET_RESTATEMENT',value:e.target.value})} helperText="“学完了 / 我会了”不算解释证据。"/><Button variant="contained" disabled={!valid} onClick={()=>dispatch({type:'SET_PHASE',phase:'quiz_active'})}>用这段复述进入 Quiz</Button></section>}
