import {FormControl,InputLabel,MenuItem,Select,TextField} from '@mui/material';export interface Filters{subject:string;chapter:string;cause:string;state:string}
export function WrongQuestionFilters({value,onChange}:{value:Filters;onChange:(filters:Filters)=>void}):JSX.Element{return<div className="filters"><TextField label="学科" value={value.subject} onChange={(e)=>onChange({...value,subject:e.target.value})}/><TextField label="章节" value={value.chapter} onChange={(e)=>onChange({...value,chapter:e.target.value})}/><TextField label="错因" value={value.cause} onChange={(e)=>onChange({...value,cause:e.target.value})}/><FormControl><InputLabel>复测状态</InputLabel><Select label="复测状态" value={value.state} onChange={(e)=>onChange({...value,state:e.target.value})}><MenuItem value="">全部</MenuItem><MenuItem value="candidate-not-scheduled">建议待确认</MenuItem></Select></FormControl></div>}
