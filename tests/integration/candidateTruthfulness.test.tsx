import {describe,expect,it} from 'vitest';import {render,screen} from '@testing-library/react';import {ModeBanner} from '../../src/components/ModeBanner';import {ProposalBadge} from '../../src/components/ProposalBadge';
describe('candidate truthfulness',()=>{it('renders truthful state labels',()=>{render(<><ModeBanner/><ProposalBadge/></>);expect(screen.getByText(/所有结果为候选/)).toBeInTheDocument();expect(screen.getByText(/尚未提交/)).toBeInTheDocument();const text=document.body.textContent??'';for(const forbidden of ['已保存','已归档','已掌握','已入队'])expect(text).not.toContain(forbidden)});});
