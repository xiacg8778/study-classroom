import{describe,expect,it}from'vitest';import{inspectImageHeader}from'../../src/adapters/tesseract/imageBoundary';
const png=(width:number,height:number):Uint8Array=>{const bytes=new Uint8Array(24);bytes.set([0x89,0x50,0x4e,0x47,0x0d,0x0a,0x1a,0x0a]);new DataView(bytes.buffer).setUint32(16,width);new DataView(bytes.buffer).setUint32(20,height);return bytes;};
describe('local image OCR boundary',()=>{it('accepts matching PNG metadata and pixels',()=>{const file=new File([png(1200,800).buffer as ArrayBuffer],'question.png',{type:'image/png'});expect(inspectImageHeader(file,png(1200,800))).toEqual({kind:'png',width:1200,height:800});});it('rejects disguised image content',()=>{const file=new File([png(1200,800).buffer as ArrayBuffer],'question.jpg',{type:'image/jpeg'});expect(()=>inspectImageHeader(file,png(1200,800))).toThrow('IMAGE_TYPE_MISMATCH');});it('rejects excessive decoded pixels',()=>{const file=new File([png(8000,8000).buffer as ArrayBuffer],'question.png',{type:'image/png'});expect(()=>inspectImageHeader(file,png(8000,8000))).toThrow('IMAGE_LIMIT_EXCEEDED');});});
